version: "3.9"

services:
  dev:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: dotnet-sdk-9-preview
    volumes:
      - ./:/course
    working_dir: /course
    command: [ "bash" ]
    tty: true
